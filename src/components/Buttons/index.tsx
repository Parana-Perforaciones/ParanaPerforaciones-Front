import React from 'react';
import Button from '@mui/material/Button';

const Botao = () => {
  return (
    <Button
      variant="contained"
      color="primary"
    >
      Comment
    </Button>
  );
};

export default Botao;
